# Vercel Blob — auto-added when you connect a Blob store in the Vercel project
BLOB_READ_WRITE_TOKEN=

# Shared secret for POST /api/snapshot (bot ingest). Must match the engine's config.
# Send as header: x-bot-ingest-secret
BOT_INGEST_SECRET=
